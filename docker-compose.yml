services:
  gateway:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - docs
      - frontend

  docs:
    build:
      context: ./docs
    expose:
      - "8080"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/postgres?sslmode=disable
    develop:
      watch:
        - action: rebuild
          path: ./docs

  frontend:
    build:
      context: ./frontend
    expose:
      - "8080"
    environment:
      API_BASE_URL: http://docs:8080
    develop:
      watch:
        - action: rebuild
          path: ./frontend

  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    expose:
      - "5432"
    ports:
      - "5432:5432"

volumes:
  pgdata:
